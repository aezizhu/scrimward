---
name: list
description: List the custom Redactly redaction rules.
---

# List custom redaction rules

Run:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/redactly-py" rules list
```

Show the user their rules. (Built-in detectors — API keys, emails, cards, etc. — are always on and
are not listed here.)
